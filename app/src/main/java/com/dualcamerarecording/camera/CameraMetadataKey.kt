package com.dualcamerarecording.camera

/**
 * Empty placeholder - camera metadata keys are accessed directly in CameraInventory.
 * The CameraCharacteristics constants FLASH_AVAILABLE and LOGICAL_MULTI_CAMERA_SUPPORTED_PHYSICAL_IDS
 * are used directly without a wrapper.
 */
object CameraMetadataKey