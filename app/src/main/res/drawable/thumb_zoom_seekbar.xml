<?xml version="1.0" encoding="utf-8"?>
<!-- Vertical "level indicator" for the zoom slider in the main activity.

     Shape: a tall thin pill (rounded rectangle, long side parallel to the
     y-axis). The short side (width) is 4dp, 1/3 of the standard SeekBar thumb
     (≈12dp), so the slider reads as a clean horizontal bar with a slim level
     marker rather than a wide knob.

     Color: ?attr/colorOnSurface, which the project theme binds to
       - Light: #12263A (near-black)
       - Dark:  #E0E0E0 (near-white)
     so the thumb stays visible against the activity background in both
     themes (the previous ?attr/colorPrimary was green in light and lavender
     in dark, and blended into the surface).

     Paired with `bg_zoom_seekbar.xml` (theme-aware progress / track). -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">
    <solid android:color="?attr/colorOnSurface" />
    <corners android:radius="2dp" />
    <size
        android:width="4dp"
        android:height="24dp" />
</shape>
